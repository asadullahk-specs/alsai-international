services:
  mongodb:
    image: mongo:7
    container_name: alsai-mongodb
    restart: unless-stopped
    ports:
      - '27017:27017'
    volumes:
      - alsai_mongo_data:/data/db

volumes:
  alsai_mongo_data:
